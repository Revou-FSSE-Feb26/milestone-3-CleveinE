import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import Providers from "@/components/Providers";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "RevoShop — Online Watch Store Indonesia",
  description:
    "Belanja jam tangan original dari 100+ brand ternama. Casio, Seiko, Daniel Wellington, Fossil, Citizen & lainnya.",
};

export default function RootLayout({ children }) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col bg-[#0a0a0f] text-zinc-100">
        <Providers>{children}</Providers>
      </body>
    </html>
  );
}
